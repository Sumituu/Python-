# Calculate the average of values in a dictionary.
# Sample dictionary with numeric values
my_dict = {'a': 10, 'b': 20, 'c': 30, 'd': 40}

# Calculate the average of values in the dictionary
total = sum(my_dict.values())
average = total / len(my_dict)

# Print the average
print("Average of Values:", average)
