# Find the union of two sets
# Create two sets
set1 = {1, 2, 3, 4, 5}
set2 = {3, 4, 5, 6, 7}

# Find the union of the two sets
union_set = set1.union(set2)
#or can use this below cmd
#union_set = set1 | set2

# Print the result
print(union_set)
