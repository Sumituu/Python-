# Check if two lists are equal (have the same elements in the same order).
# Sample lists
list1 = [1, 2, 3, 4, 5]
list2 = [1, 2, 3, 4, 5]

# Check if the two lists are equal
if list1 == list2:
    print("The two lists are equal (have the same elements in the same order).")
else:
    print("The two lists are not equal.")
